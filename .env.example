# Server
PORT=3000
LOG_LEVEL=info
APP_VERSION=0.1.0

# Profile: mvp | v2
BRIDGE_PROFILE=mvp
# PUBLISH_MODE=comment       # optional: comment | review (derived from profile if unset)
# BRANCH_SYNC_STRATEGY=mirror # optional: mirror | git (derived from profile if unset)

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/reviewrelay

# Gitea
GITEA_BASE_URL=https://gitea.example.com
GITEA_TOKEN=your-gitea-token
GITEA_WEBHOOK_SECRET=your-gitea-webhook-secret
# GITEA_WEBHOOK_AUTH_HEADER=  # optional: exact value of Authorization header

# GitHub
GITHUB_TOKEN=your-github-token
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret

# Repo mapping (JSON array)
REPO_MAP=[{"gitea":"org/repo","github":"org/repo-mirror"}]

# Copilot detection
COPILOT_REVIEWER_LOGINS=github-copilot[bot],copilot

# Smee.io proxy (desarrollo local — reenvía webhooks de GitHub a localhost)
# 1. Crea un canal en https://smee.io/new
# 2. Usa esa URL como Payload URL del webhook en GitHub
# 3. Ejecuta: smee -u <SMEE_URL> --target http://localhost:3000/webhooks/github

# Git sync (only if BRANCH_SYNC_STRATEGY=git)
# GIT_CACHE_DIR=/var/lib/reviewrelay/git-cache
# GITEA_GIT_URL_TEMPLATE=https://{token}@gitea.example.com/{owner}/{repo}.git
# GITHUB_GIT_URL_TEMPLATE=https://{token}@github.com/{owner}/{repo}.git
# GITEA_GIT_TOKEN=
# GITHUB_GIT_TOKEN=
